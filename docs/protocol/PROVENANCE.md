# Protocol document provenance

`rapp-v26.8.16.85.md` and `rapp-state-machine-v26.8.16.85.yaml` are copied
verbatim from the ReFineID monorepo public protocol tree at commit `1d78e68`
("Revise RAPP draft to 26.8.16.85", 2026-08-16). The monorepo copy is the
original; revisions land there first and are re-vendored here by commit. The
implementation in `crates/refineid-rapp-core` tracks exactly the vendored
revision named in its documentation.
