# Protocol document provenance

`rapp-v26.9.4.181.md`, `rapp-state-machine-v26.9.4.181.yaml`, and
`vectors/rapp-v26.9.4.181.json` are copied verbatim from the RefineID
project's canonical protocol tree, document version 26.9.4.181
(2026-09-04). The canonical copy is the original; revisions land there
first and are re-vendored here by commit. The implementation in
`crates/refineid-rapp-core` tracks exactly the vendored revision named in
its documentation, and the vectors file is the conformance corpus its
tests replay.
